{
  "name": "env-leak-detector",
  "version": "1.0.0",
  "description": "Scan git history for accidentally committed secrets and API keys",
  "main": "src/server.js",
  "bin": {
    "env-leak-detector": "src/cli.js"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "cli": "node src/cli.js"
  },
  "keywords": ["security", "git", "secrets", "api-keys", "env", "leak", "scanner"],
  "author": "Tushar",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mongoose": "^8.0.0",
    "simple-git": "^3.20.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
